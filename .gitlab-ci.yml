image: php:5.6

before_script:
  - ci/docker_install.sh
  - ci/composer_installer.sh
  - composer install

test:
  script:
  - vendor/bin/phpcs --standard=PSR2 lib/ src/ tests/ web/
  - vendor/bin/phpunit
